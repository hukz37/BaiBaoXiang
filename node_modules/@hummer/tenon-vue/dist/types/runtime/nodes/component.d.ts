import { Base } from './Base';
export interface NativeComponent {
    name: string;
    factory: Function;
}
export declare function register(component: NativeComponent | Array<NativeComponent>): void;
export declare function getComponent(tag: string): Base;
