import { createStore, Store } from './store';
import { useStore } from './injectKey';
import { mapState, mapMutations, mapGetters, mapActions, createNamespacedHelpers } from './helpers';
import { createLogger } from './plugins/logger';
import { createHummerPlugin } from './plugins/hummer';
declare const _default: {
    createStore: typeof createStore;
    Store: typeof Store;
    useStore: typeof useStore;
    mapState: (namespace: any, map: any) => any;
    mapMutations: (namespace: any, map: any) => any;
    mapGetters: (namespace: any, map: any) => any;
    mapActions: (namespace: any, map: any) => any;
    createNamespacedHelpers: (namespace: any) => {
        mapState: (map: any) => any;
        mapGetters: (map: any) => any;
        mapMutations: (map: any) => any;
        mapActions: (map: any) => any;
    };
    createLogger: typeof createLogger;
    createHummerPlugin: typeof createHummerPlugin;
};
export default _default;
export { createStore, Store, useStore, mapState, mapMutations, mapGetters, mapActions, createNamespacedHelpers, createLogger, createHummerPlugin };
